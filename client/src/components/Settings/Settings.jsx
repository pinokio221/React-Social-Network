import React from 'react';
import styles from './Settings.module.css'

const Settings = (props) => {
    return (
        <div>
            <span>Settings should be here</span>
        </div>
    )
}

export default Settings;